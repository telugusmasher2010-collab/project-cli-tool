fn main() {
    // Frontend entry point placeholder.
    // The actual React app is served by Vite during development
    // and bundled into dist/ for production builds.
    println!("{{project_name}} - Frontend placeholder");
}
